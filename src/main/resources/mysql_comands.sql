# insert roles (id, name) values (0, 'ADMIN');
# select *from roles;
# select *from users;
# insert roles (id, name) values (1, 'USER');
# update roles SET name='USER' where id=2;
# insert user_roles (user_id, role_id) values (1, 1);
# insert user_roles (user_id, role_id) values (1, 2);
# select *from user_roles;
